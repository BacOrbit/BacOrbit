# لوحة إدارة BacOrbit — دليل التركيب والخطوات اليدوية

## الملفات التي تم إنشاؤها
| الملف | الوظيفة |
|---|---|
| `admin.html` | لوحة الإدارة (دخول أدمن + إحصائيات + مراجعة ملخصات + إدارة منتدى) |
| `admin.css` | تنسيقات اللوحة (تعتمد على متغيرات `style.css` نفسها) |
| `admin.js` | كل منطق اللوحة |
| `submit-summary.html` | صفحة يرفع منها المستخدم العادي ملخصه (PDF) |
| `submit-summary.js` | منطق رفع الملخص وحفظه بحالة `pending` |
| `firestore.rules` | قواعد أمان Firestore الجديدة — **يجب نسخها ولصقها يدويًا** |

ضع الملفات الخمسة الأولى في **جذر المشروع** بجانب `index.html` و`chat.html` تمامًا (نفس المستوى)، لأنها تستخدم مسارات نسبية (`style.css`, `script.js`) بنفس أسلوب باقي صفحات الموقع.

لم يتم لمس أي ملف موجود مسبقًا — كل شيء إضافات جديدة بالكامل.

---

## ⚠️ خطوات يجب تنفيذها يدويًا في Firebase Console (بالترتيب)

### 1) تفعيل تسجيل الدخول بالبريد وكلمة المرور
- Firebase Console → مشروع `bacorbit-c5d67` → **Authentication** → تبويب **Sign-in method**
- فعّل مزود **Email/Password**

### 2) إنشاء حساب الأدمن الخاص بك
- في نفس صفحة Authentication → تبويب **Users** → **Add user**
- أدخل بريدك الإلكتروني وكلمة مرور قوية خاصة بك (منفصلة تمامًا عن أي حساب آخر)
- بعد الإنشاء، انسخ **User UID** الذي يظهر بجانب الحساب (سلسلة حروف/أرقام طويلة)

### 3) تسجيلك كأدمن داخل Firestore
- Firebase Console → **Firestore Database** → **Start collection** (أو استخدم مجموعة موجودة إن ظهرت)
- اسم المجموعة (Collection ID): `admins`
- Document ID: **الصق UID الذي نسخته في الخطوة السابقة بالضبط** (وليس اسمًا عشوائيًا)
- أضف أي حقل بسيط مثل: `email` (string) = بريدك، فقط لتوثيق من صاحب الحساب — الحقل غير إلزامي تقنيًا لكنه مفيد للمراجعة لاحقًا

هذه الوثيقة (`admins/{UID}`) هي **مصدر الصلاحية الحقيقي**، وليس أي شيء في الكود.

### 4) تفعيل مجموعة Storage للملخصات (إن لم تكن مفعّلة أصلًا)
- إن كان **Storage** مفعّلاً بالفعل في المشروع (يبدو أنه كذلك بما أن المنتدى يرفع مرفقات)، لا حاجة لأي خطوة إضافية.

### 5) نشر قواعد الأمان الجديدة (الأهم أمنيًا)
- Firebase Console → **Firestore Database** → تبويب **Rules**
- افتح ملف `firestore.rules` المرفق، انسخ **كامل محتواه**، والصقه مستبدلًا المحتوى الحالي بالكامل
- اضغط **Publish**

> ⚠️ إن كانت لديك قواعد مخصصة سابقة لم تُشاركها معي، تأكد من مراجعتها قبل الاستبدال حتى لا تفقد أي حماية كانت موجودة مسبقًا. بما أنك ذكرت أنك لا تعرف حالتها الحالية، الأرجح أنها لا تزال الوضع الافتراضي التجريبي (مفتوح لكل مصادقة)، وملف `firestore.rules` المرفق أكثر أمانًا من ذلك بكثير.

---

## ما الذي تفعله القواعد الجديدة فعليًا (بدون أي مصطلحات تقنية معقدة)
- لا أحد يستطيع حذف منشور غير منشوره في المنتدى، ولا أحد سوى الأدمن يستطيع الحذف النهائي من قاعدة البيانات.
- لا أحد يستطيع قراءة ملخص لم يُعتمد بعد إلا صاحبه أو الأدمن — فلن يظهر أي ملخص للعموم قبل الموافقة عليه.
- لا أحد يستطيع تعديل حالة الملخص (`pending/approved/rejected`) من المتصفح إلا الأدمن، حتى لو حاول تعديل الطلب يدويًا من كود المتصفح.
- وثيقة `admins/{uid}` لا يمكن إنشاؤها أو تعديلها من أي عميل إطلاقًا — فقط أنت من Firebase Console يستطيع إضافة أدمن جديد مستقبلًا.

---

## نقطة أمان يجب أن تعرفها (وليست خطأ في الكود، بل قيد بنيوي حالي)
نظام **حظر المستخدم بعد 3 بلاغات** الموجود أصلًا في المنتدى (`chat.html`) يعمل حاليًا بالكامل من طرف المتصفح (client-side transaction). قواعد الأمان الجديدة تسمح بتحديث حقل `banned` لأي مستخدم آخر عند تحديث حقول البلاغ فقط، حفاظًا على عمل هذه الميزة كما هي دون تعطيلها — لكنها لا تستطيع أن تتحقق من جهة الخادم أن "عدد البلاغات فعلاً وصل 3" قبل السماح بالحظر (لأن ذلك يتطلب Cloud Function منفصلة، وهي خارج نطاق هذا الطلب). هذا القيد كان موجودًا أصلًا في تصميم المشروع قبل لوحة الإدارة، ولم أزده سوءًا، لكن من المفيد معرفته إن أردت تعزيزه لاحقًا عبر Cloud Functions.

---

## إضافة رابط "رفع ملخص" و"لوحة الإدارة" لقائمة التنقل (اختياري)
لم أعدّل أي صفحة موجودة تلقائيًا حفاظًا على طلبك بعدم لمس الملفات الحالية. إن أردت إضافة رابط "رفع ملخص" في القائمة المنسدلة (☰) بأي صفحة، أضف هذا السطر داخل `nav-menu-panel` في الصفحة المطلوبة:

```html
<a href="submit-summary.html" class="nav-menu-item" role="menuitem"><span class="nav-menu-item-icon">📤</span>رفع ملخص</a>
```

**لا تضف رابط `admin.html` في أي قائمة تنقل عامة** — الحماية الحقيقية من Firestore Rules، لكن لا داعي لتسهيل اكتشاف الرابط لغير الأدمن. احتفظ به كرابط مباشر تفتحه بنفسك فقط.

---

## ما لم يُنفَّذ بعد (خارج الطلب الحالي أو يحتاج قرارًا منك)
1. **عرض الملخصات المعتمدة للعموم**: حاليًا عند القبول تصبح حالة الملخص `approved` في Firestore وجاهزة، لكن لا توجد بعد صفحة تعرض هذه الملخصات المعتمدة للزوار (مثل قسم جديد في `library.html`). هذا يحتاج خطوة تالية منفصلة إن أردتها.
2. **نظام البلاغات والحظر الكامل من لوحة الإدارة**: البنية جاهزة (تظهر البلاغات في الإحصائيات وبجانب كل منشور)، لكن أزرار "حظر المستخدم" المباشرة من اللوحة نفسها لم تُضف بعد كما طلبت أن أجهّز النظام لذلك مستقبلًا فقط.
3. **Cloud Functions** لتحقق خادمي أقوى من شرط البلاغات (مذكور أعلاه).

---

## بعد إتمام الخطوات اليدوية
1. ارفع الملفات الخمسة إلى مستودع GitHub Pages الخاص بك (بجانب باقي الصفحات).
2. افتح `https://bacorbit.github.io/BacOrbit/admin.html` وسجّل الدخول بالحساب الذي أنشأته.
3. جرّب من `submit-summary.html` رفع ملخص تجريبي، وتحقق من ظهوره في تبويب "الملخصات" داخل اللوحة، ثم جرّب القبول والرفض.
4. أخبرني بأي خطأ يظهر فعليًا (رسالة الخطأ من الـ Console) وسأصلحه فورًا.
