# الدراسة لاحقًا + الإشعارات + إضافة ملفات — دليل التركيب

## كيف فهمتُ المشروع قبل التعديل
- **الهوية**: كل تلميذ له UID عبر Firebase Anonymous Auth، ثابت لكل متصفح/جهاز (نفس آلية المنتدى تمامًا)، مع وثيقة `users/{uid}` تحمل الاسم العشوائي وحالة `banned`.
- **المنتدى**: `forumPosts` (منشورات وردود بحقل `parentId`)، بلا أي نظام إشعارات مركزي (الموجود سابقًا في `chat.html` هو Notification API الخاص بالمتصفح فقط، محلي ومؤقت، وليس مخزَّنًا في Firestore).
- **المراجعة**: `summaries` + لوحة `admin.html`/`admin.js` (قبول/رفض/حذف)، والصلاحية الحقيقية تأتي من وجود وثيقة `admins/{uid}`.
- **الشعب والمواد**: مأخوذة من صفحات `Branches/1_*.html` الفعلية (لا توجد بيانات JSON مركزية لها).
- بناءً على هذا الفهم، تم **دمج** كل ميزة جديدة مع ما هو موجود بدل اختراع نظام مواز.

---

## الملفات الجديدة

| الملف | الوظيفة |
|---|---|
| `firebase-shared.js` | تحميل كسول لـ Firebase + هوية Anonymous موحّدة، لأي صفحة لا تُدير Firebase بنفسها |
| `notifications.js` | زر 🔔 + لوحة الإشعارات (استماع لحظي لمجموعة `notifications`) |
| `study-later.js` | زر "الدراسة لاحقًا" داخل صفحات الدروس + منطق التذكير (حتى 3 مرات) |
| `add-files.html` / `add-files.js` | صفحة جديدة: شعبة ← مادة ← صنف (ملخص/تمارين) ثم رفع PDF |
| `firestore.rules` | نسخة كاملة محدَّثة من القواعد (تشمل كل المجموعات القديمة والجديدة) |

## الملفات المعدَّلة

| الملف | التعديل |
|---|---|
| `script.js` | تحميل ديناميكي لـ 🔔 والدراسة لاحقًا في كل صفحة **لا** تُدير Firebase بنفسها؛ إضافة رابط "إضافة ملفات" لكل قائمة تنقل تلقائيًا |
| `chat.html` | تحميل `notifications.js`، إنشاء إشعار لصاحب الموضوع عند أي رد جديد (بمعرّف ثابت `fr_<replyId>` يمنع التكرار تمامًا)، تعليم كل منشور بـ `id="post-<id>"`، والانتقال التلقائي إليه عند فتح رابط إشعار |
| `admin.js` | عرض شارتي "الشعبة" و"الصنف" للملفات المرسلة من الصفحة الجديدة (نفس تبويب الملخصات) |
| `admin.html` | تعديل نصي بسيط فقط ("الملخصات" ← "الملخصات والملفات") |

**لم يتم لمس أي تصميم أو ألوان أو خطوط أو بنية HTML قائمة خارج هذه النقاط.**

---

## لماذا لم تُعدَّل صفحات الدروس (L_*.html) واحدة واحدة؟
بدل تعديل عشرات ملفات الدروس يدويًا، استُخدم نفس أسلوب الموقع الحالي في `script.js` (كما فعل تمامًا مع أيقونة الموقع/favicon وقائمة ☰): أي صفحة تحتوي حاوية `.lessons` تحصل تلقائيًا على زر "📚 الدراسة لاحقًا"، وأي صفحة في الموقع تحصل تلقائيًا على 🔔 ورابط "إضافة ملفات" — دون أي تكرار للكود أو خطر نسيان صفحة.

**استثناء مقصود:** الصفحات التي تُحمِّل Firebase SDK بنفسها أصلًا (`chat.html`, `admin.html`, `submit-summary.html`, `add-files.html`) **لا** تستخدم التحميل التلقائي (`firebase-shared.js`) لأن استدعاء `firebase.initializeApp()` مرتين يُسبب خطأ يوقف الصفحة بالكامل. لذلك أُضيف `notifications.js` يدويًا فقط في `chat.html` و`add-files.html` (اللتين تحتاجانه فعليًا)، بينما `submit-summary.html` و`admin.html` تُركتا كما هما (الأولى لا تحتاج إشعارات، والثانية حساب أدمن منفصل تمامًا عن حسابات الطلبة).

---

## ⚠️ خطوات يجب تنفيذها يدويًا

### 1) نشر قواعد Firestore الجديدة
- Firebase Console → **Firestore Database** → **Rules**
- انسخ **كامل محتوى** `firestore.rules` المرفق والصقه مستبدلًا القواعد الحالية بالكامل، ثم **Publish**.
- هذه النسخة تحتوي على كل القواعد القديمة (forumPosts / summaries / admins / users) **بالإضافة** إلى قواعد `savedLessons` و`notifications` الجديدة. إن كانت قواعدك الحالية تحتوي تفاصيل إضافية لم تُشاركها معي، راجعها قبل الاستبدال.

### 2) رفع الملفات إلى جذر المشروع
ضع كل الملفات المذكورة أعلاه (الجديدة والمعدَّلة) في **جذر المشروع** بجانب `index.html` مباشرة (`add-files.html/js` و`notifications.js` و`study-later.js` و`firebase-shared.js` كلها ملفات جذرية، وليست داخل `Branches/`).

### 3) لا حاجة لأي تعديل في Firebase Storage
`add-files.js` يرفع الملفات إلى نفس المسار `summaries/{uid}/...` الذي تستخدمه `submit-summary.js` بالفعل، فإن كانت قواعد Storage الحالية تسمح برفع الملخصات، فهي تعمل هنا مباشرة دون أي تعديل.

### 4) لا حاجة لأي فهرس (index) يدوي في الغالب
الاستعلامات الجديدة (`notifications where uid == X orderBy createdAt`، و`savedLessons where uid == X and reminderCount < 3`) تحتاج فهارس مركّبة (composite index) في أغلب الحالات. إن ظهرت رسالة خطأ في الـ Console تحتوي رابطًا لإنشاء الفهرس تلقائيًا (`The query requires an index...`)، اضغط الرابط فقط ليُنشئه Firebase تلقائيًا — لا حاجة لإنشائه يدويًا من الصفر.

---

## قيد معروف (وليس خطأ في الكود)
هوية Anonymous Auth مرتبطة بالمتصفح/الجهاز نفسه (نفس القيد الموجود أصلًا في هوية المنتدى قبل هذا التعديل). لذلك عدّاد تذكيرات "الدراسة لاحقًا" **يبقى صحيحًا ولا يُعاد من الصفر** طالما استخدم التلميذ نفس المتصفح، لكنه لا "يتبع" التلميذ تلقائيًا عند الانتقال لجهاز أو متصفح مختلف تمامًا — لأن الموقع بالكامل لا يملك نظام تسجيل دخول حقيقي (بريد/كلمة مرور) للطلاب أصلًا، فقط للأدمن.
